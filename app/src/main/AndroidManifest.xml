<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cg.project.searchin">
    <!-- Internet Permission -->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>









    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".service.ChatActivity"
            android:theme="@style/AppThemeNo"/>
        <activity android:name=".ui.UserAdditionalDetails" />
        <activity android:name=".service.AddPostActivity" />
        <activity android:name=".ui.LoginOption" />
        <activity android:name=".service.JobPost" />
        <activity android:name=".ui.OrganisationProfile" />
        <activity android:name=".ui.OrganisationLogin" />
        <activity android:name=".ui.UserLogin" /> <!-- <activity -->
        <!-- android:name=".ui.login.LoginActivity" -->
        <!-- android:label="@string/title_activity_login" /> -->
        <activity android:name=".ui.RegisterUser" />
        <activity android:name=".ui.RegisterOrganisation" />
        <activity android:name=".ui.Home" />
        <activity android:name=".ui.RegisterOption" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>